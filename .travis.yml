language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-frameworkless' ]; then cd .. && eval "mv $currentfolder generator-frameworkless" && cd generator-frameworkless; fi

